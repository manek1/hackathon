def check_all_num(input_string):
    """
    :param input_string:
    :return: Checks whether the given string contains only alphanumeric characters if yes 1 if no 0
    """
    if isInteger(input_string):
        return 1
    else:
        return 0
